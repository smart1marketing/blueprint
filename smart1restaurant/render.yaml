services:
  - type: web
    name: smart1restaurant
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 300
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.4
      - key: OPENAI_API_KEY
        sync: false
      - key: OPENAI_MODEL
        value: gpt-4.1-mini
      - key: GHL_WEBHOOK_URL
        sync: false
      - key: CLOUDINARY_URL
        sync: false
      - key: REPORT_NAME
        value: restaurant-market-report
      - key: ENABLE_PDF
        value: "1"
      - key: ALLOWED_ORIGINS
        value: "*"
      # Optional: only used as a fallback public URL when CLOUDINARY_URL is not set.
      - key: PUBLIC_BASE_URL
        sync: false
