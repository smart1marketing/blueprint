services:
  - type: web
    name: knack-creative-lookup
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run serve
    
    envVars:
      - key: REACT_APP_KNACK_API_KEY
        scope: build
        sync: false
      - key: NODE_ENV
        value: production
    
    # Auto-deploy on push to main
    autoDeploy: true
